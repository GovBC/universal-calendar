# universal-calendar
Universal Calendar PWA with astronomy, prayer alerts, lunar visibility, seasons, and multi-calendar support.

The published static PWA lives in `dist/`. Capacitor builds iOS and Android from a native copy produced by `npm run build`; see [the mobile build guide](docs/CAPACITOR.md). The existing `android-app/` implementation is retained separately.
